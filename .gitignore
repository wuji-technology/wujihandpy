.vscode/
.cache/

.venv/

build/
package-build/
wheelhouse/
dist/
_build/
_generate/
*.so
*.py[cod]
*.egg-info
*env*

compile_commands.json

src/wujihandpy/_version.py

docs/plans/

# Local-only experiments. HIL scripts (tests/hil_*.py) ARE committed —
# they need a connected board so they're gated behind WUJIHAND_HIL=1
# (see tests/README.md). Use the .local_*.py prefix for personal one-offs.
tests/.local_*.py

# Claude-mem generated files (keep root CLAUDE.md)
**/CLAUDE.md
!/CLAUDE.md