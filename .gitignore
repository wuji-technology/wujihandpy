.vscode/
.cache/

.venv/

build/
package-build/
wheelhouse/
dist/
_build/
_generate/
*.so
*.py[cod]
*.egg-info
*env*

compile_commands.json

src/wujihandpy/_version.py

docs/plans/

# HIL test scripts are local-only (need a physical tactile board, and
# this is a public repo — we don't ship hardware-specific scripts).
# Run them locally via `WUJIHAND_HIL=1 python tests/hil_sdk.py`.
tests/hil_*.py
tests/.local_*.py

# Claude-mem generated files (keep root CLAUDE.md)
**/CLAUDE.md
!/CLAUDE.md